'''
Register the model with the django settings.
'''
from django.apps import AppConfig


class LivetimesConfig(AppConfig):
    '''
    Class used to initialise the livetimes app with the settings.
    '''
    name = 'livetimes'
